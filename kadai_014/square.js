// 1. 引数で渡した値を二乗する関数
function square(number) {
    return number * number;
}

// 引数の例（この値は自由に変更可能です）
const input = 10;

// 2. 関数を呼び出し、結果をコンソールに出力
const result = square(input);
console.log(`${input}の二乗は${result}です。`);
